# portfolio-watch 擴充規格書
## 建倉排程（Build Schedule）＋ 偏離帶再平衡（Threshold-Band Rebalance）

**版本**：Spec v1.0
**目標讀者**：Claude Code（實作者）
**現有專案**：`chiling222/portfolio-watch`（GitHub Pages PWA，目前 v2.2）
**現有技術**：純 vanilla HTML/CSS/JS、localStorage、GitHub Actions 每日更新報價（TWSE/TPEx/Stooq）、FinMind API 即時查詢

---

## 0. 文件目的

在現有 portfolio-watch 之上，新增兩個模組：

1. **建倉排程**：協助使用者依自訂計畫、分批把資金建立到目標配置，並追蹤進度。
2. **偏離帶再平衡儀表板**：自動計算目前各資產塊佔比、與目標的偏離，穿出容忍帶時提示對應動作。

本規格描述功能、資料結構、計算邏輯、UI 與驗收條件，詳細度以「可直接實作」為準。實作者可自行決定檔案切分與函式命名，但須符合本規格定義的資料結構與行為。

---

## 1. 設計原則（實作時的最高指導）

1. **框架開放、不寫死**：配置框架（原型/正二/現金的比例）由使用者選擇或自訂，**不得寫死為某一組數字**（如 613）。需支援常見預設（703/433/613/442/505…）與完全自訂。
2. **標的不寫死**：每個資產塊底下綁哪些標的，由使用者設定。正二可以是任何槓桿型 ETF；現金塊可以是短債 ETF、貨幣市場基金，**也可以是沒有報價的定存（手動輸入金額）**。
3. **復用既有資料層**：目前各塊的「現值」應由 app 既有的持倉＋報價資料自動算出，**使用者不需手動輸入現值**。實作時透過既有資料存取層取得每檔標的的股數與現價。
4. **檢視 ≠ 交易**：儀表板只負責「顯示狀態、提示動作」，**不自動下單、不修改持倉**。實際買賣由使用者在券商完成後，回來手動標記。
5. **不對稱動作**：穿上緣（漲多）＝可執行的「修剪」；穿下緣（跌破）＝僅提醒、不催促。（詳見 §8.4）
6. **延續現有技術**：純 vanilla JS ＋ localStorage，單檔可部署，**不得引入建置工具或外部框架**，不得改動既有 GitHub Actions 報價流程。
7. **風格一致**：延續現有米色暖光主題、放大字級，優先適配 Mac 與 iPad。

---

## 2. 範圍

### In scope
- 設定頁：配置框架、標的分層對應、手動現金、帶寬、建倉排程參數。
- 建倉排程模組（含進度追蹤、本月待投、回檔加碼提示、完成歸檔）。
- 偏離帶再平衡儀表板模組（含帶狀量尺、狀態、不對稱動作、年度保底提醒）。
- 上述資料以 localStorage 持久化。

### Out of scope（非目標）
- 自動下單／串接券商交易。
- 自動修改使用者持倉數量（持倉來源仍是既有機制）。
- 稅務試算、報酬預測、退休現金流模型（那些在別處處理）。
- 強制原型內部多標的（如 006208/00662）的再平衡（見 §8.6）。

---

## 3. 名詞定義

| 名詞 | 定義 |
|---|---|
| **資產塊 / sleeve** | 配置的組成單位，典型為「原型 / 正二 / 現金」三塊，但數量與名稱可自訂。 |
| **核心 core** | 納入再平衡計算的所有資產塊之總和（即配置框架涵蓋的部分）。 |
| **衛星 / excluded** | 標記為排除的長抱部位，**不計入核心、不參與再平衡**，僅可另行顯示。 |
| **目標比例 target** | 使用者設定的各塊應占核心的百分比，全部塊加總須 = 100%。 |
| **偏離帶 band** | 目標 ± 帶寬（預設絕對 ±5%）。目前比例落在帶內＝正常，穿出＝觸發。 |

---

## 4. 資料模型（localStorage）

新增下列 key，皆存 JSON 字串。欄位命名為建議，實作可微調但須維持語意與型別。

### 4.1 `pw_allocation_plan`

```json
{
  "version": 1,
  "framework": {
    "presetName": "613",
    "sleeves": [
      { "id": "core",     "label": "原型", "target": 0.60 },
      { "id": "leverage", "label": "正二", "target": 0.10 },
      { "id": "cash",     "label": "現金", "target": 0.30 }
    ]
  },
  "bandWidth": 0.05,
  "tickerMap": {
    "006208": "core",
    "00662":  "core",
    "00631L": "leverage",
    "00865B": "cash",
    "2330":   "excluded"
  },
  "manualCash": [
    { "id": "mc_1", "label": "台幣定存", "amount": 3000000, "sleeveId": "cash" }
  ],
  "lastRebalanceDate": "2026-01-15"
}
```

**規則**
- `sleeves[].target` 全部加總必須 = 1.0（誤差容忍 ±0.001），否則設定頁不得存檔並顯示錯誤。
- `sleeves` 陣列長度不固定（開放框架）；`id` 唯一。
- `tickerMap`：value 為某個 `sleeve.id`，或字串 `"excluded"`（衛星）。未出現在此表的持倉，預設視為 `"excluded"`（不計入核心），並在設定頁提示使用者尚未分類。
- `manualCash`：無報價的現金（定存等），`amount` 為新台幣整數，`sleeveId` 通常指向現金塊。
- `bandWidth`：絕對百分比（0.05 = ±5%），使用者可改。
- `lastRebalanceDate`：ISO 日期字串，供年度保底提醒計算。

### 4.2 `pw_build_schedule`

```json
{
  "version": 1,
  "enabled": true,
  "startDate": "2026-03-01",
  "plans": [
    { "sleeveId": "cash",     "mode": "lumpsum", "done": true },
    { "sleeveId": "core",     "mode": "dca", "months": 12, "amountPerMonth": 75000, "completedBatches": 3, "dipRule": null },
    { "sleeveId": "leverage", "mode": "dca", "months": 12, "amountPerMonth": 12500, "completedBatches": 2,
      "dipRule": { "enabled": true, "dropPct": 0.05 } }
  ]
}
```

**規則**
- `mode`：`"lumpsum"`（一次到位，用 `done` 布林追蹤）或 `"dca"`（分批）。
- DCA 塊：`months` 總期數、`amountPerMonth` 每期金額、`completedBatches` 已完成期數（0 ≤ completedBatches ≤ months）。
- `dipRule`（可選，通常給正二）：`dropPct` 為觸發回檔加碼的跌幅門檻（0.05 = 5%）。`null` 表示不啟用。
- 所有參數皆由使用者在設定頁自填。

---

## 5. 設定頁（Settings）

新增或擴充設定區，包含四組：

### 5.1 配置框架
- 提供預設下拉：`703 / 433 / 613 / 442 / 505 / 自訂`。選預設時自動帶入對應 sleeves target。
  - 預設值（原型:正二:現金）：703 = 70/0/30、433 = 40/30/30、613 = 60/10/30、442 = 40/40/20、505 = 50/0/50。
- 選「自訂」時，可新增/刪除資產塊、編輯 label 與 target。
- **即時驗證**：target 加總須 = 100%，否則以紅字提示且禁止存檔。

### 5.2 標的分層對應
- 列出使用者目前所有持倉標的（來自既有資料層）＋ 手動現金項目。
- 每一項可指派到某個資產塊，或標記為「衛星（排除）」。
- 未分類者以醒目色標示，提醒使用者處理。

### 5.3 手動現金
- 可新增多筆 `{ label, amount, sleeveId }`，用於定存等無報價資產。

### 5.4 偏離帶與建倉參數
- `bandWidth` 可調（預設 ±5%）。
- 建倉排程：每個資產塊設定 `mode`、（DCA 時）`months`、`amountPerMonth`、`dipRule`；設定 `startDate`。

---

## 6. 模組一：建倉排程

### 6.1 目的與生命週期
協助使用者依計畫分批把資金建立到目標配置。屬**有始有終**的任務：所有塊都建置完成後，面板應自動收合／歸檔（可保留一個入口讓使用者展開檢視歷史）。

### 6.2 計算邏輯
- 設 `elapsed = 由 startDate 到今天經過的整月數`（floor）。
- 對 DCA 塊：`dueBatches = min(elapsed + 1, months)`；`outstanding = max(0, dueBatches - completedBatches)`。
- **本月待投金額** = Σ（各 DCA 塊 `outstanding × amountPerMonth`）。
- 進度 % = `completedBatches / months`（lumpsum 塊為 done ? 100% : 0%）。
- 全部塊完成（DCA completedBatches = months 且 lumpsum done = true）→ 觸發歸檔狀態。

### 6.3 回檔加碼（dip rule）
- 對啟用 `dipRule` 的塊，取該塊主要標的近一日（或既有報價層可得的最近區間）跌幅。
- 若跌幅 ≥ `dropPct`，在該塊列顯示「回檔加碼」提示（例如：單日跌逾 5%，可提前投一批）。此為**提示**，不自動改變進度。

### 6.4 UI（參考版面）
- 頂部：整體進度（例：第 3 / 12 個月）＋「本月待投」摘要行（列出各塊金額）。
- 每個資產塊一列：名稱、進度條、進度文字（lumpsum 顯示「一次到位 ✓」；DCA 顯示「n / m 批」）。
- 每列一個「完成本月」動作：點擊使 `completedBatches += 1`（上限 months）並存檔。
- 啟用 dipRule 且觸發時，於該列顯示回檔加碼提示。

---

## 7. 模組二：偏離帶再平衡儀表板

### 7.1 目的與生命週期
**常駐主畫面**。每次開啟 app 即顯示目前配置狀態；平時圓點在帶內、無動作，穿帶才亮燈。

### 7.2 現值計算（核心，排除衛星）
1. 對每檔非排除持倉：`value = shares × price`（price 來自既有報價層）。
2. 加上 `manualCash` 中對應 sleeve 的金額。
3. 依 `tickerMap` 歸屬到各 sleeve，加總得 `sleeveValue[sleeveId]`。
4. `coreTotal = Σ sleeveValue`（**只含非排除塊；衛星完全不計入**）。
5. `currentPct[sleeveId] = sleeveValue[sleeveId] / coreTotal`。

### 7.3 帶狀量尺（每個 sleeve 一條）
- 一條水平軌，繪出：容忍帶區間（target ± bandWidth 的淺色區塊）、目標中線、目前比例的標記圓點。
- 建議每條軌以「target ± (bandWidth × 3)」作為顯示視窗，讓帶與圓點清楚可見（實作可自訂視窗範圍）。
- 狀態配色：帶內＝正常（綠）、穿上緣＝暖色（琥珀）、穿下緣＝中性（不用告警紅，見 §7.4）。

### 7.4 狀態與不對稱動作
對每個 sleeve：
- **帶內**（target − band ≤ current ≤ target + band）：顯示「在帶內 ✓」，無動作。
- **穿上緣**（current > target + band）：
  - 狀態「穿上緣」。
  - **可執行動作**：`修剪金額 = (currentPct − target) × coreTotal`，文字如「修剪鎖利約 NT$X（執行）」。
- **穿下緣**（current < target − band）：
  - 狀態「穿下緣」。
  - **僅提醒、不催促**：文字如「僅提醒：可選擇不接刀」。可選擇性附上「若要拉回需買進約 NT$X」，但**須明確標示為非強制**。

> 設計理由（供實作者理解語氣）：上緣＝控制風險，鼓勵執行；下緣＝崩盤中加碼槓桿反人性，使用者可選擇不做。**下緣不得使用告警／紅色催促樣式。**

### 7.5 年度保底提醒
- `monthsSinceLastRebalance = 今天 − lastRebalanceDate`（月）。
- 於面板顯示「距上次再平衡 n 個月」。當 n ≥ 12，顯示「年度保底檢查」提示（無論是否穿帶）。
- 提供一個「已完成再平衡」動作：點擊將 `lastRebalanceDate` 更新為今天並存檔。

### 7.6 原型內部多標的處理（重要）
- 一個 sleeve（尤其原型）可含多檔標的（如 006208 ＋ 00662）。儀表板**以 sleeve 為單位聚合佔比**。
- **不對原型內部做強制再平衡**：原型為長期持有、只進不出，內部各標的比例允許自然浮動。
- 可（可選）在原型列下方以「僅供參考」方式顯示內部各標的佔比，但**不產生任何「拉回內部比例」的動作提示**。

---

## 8. UI / UX 要求

- 沿用現有米色暖光主題與放大字級；新面板視覺需與既有頁面一致。
- 兩個模組建議各自為獨立區塊：建倉可收合、完成後歸檔；再平衡常駐。
- 主要適配 Mac 與 iPad（較寬版面優先，但需在窄螢幕可用）。
- 所有金額以新台幣整數顯示（千分位）。
- 「檢視」與「動手」在視覺上分離：狀態顯示為主，動作按鈕為輔。

---

## 9. 驗收條件（Acceptance Criteria）

1. 設定頁可選預設框架或自訂；target 加總 ≠ 100% 時禁止存檔並提示。
2. 標的可分派到各 sleeve 或標記排除；排除項不影響核心佔比。
3. 手動現金（無報價）能正確計入指定 sleeve。
4. 再平衡儀表板的 `currentPct` 僅以核心（排除衛星）為分母計算正確。
5. 穿上緣顯示可執行的修剪金額；穿下緣顯示僅提醒、且非紅色告警樣式；帶內無動作。
6. 年度保底：距上次再平衡 ≥ 12 個月時顯示提示；「已完成再平衡」可更新日期。
7. 建倉排程：本月待投金額、進度、完成批次、回檔加碼提示皆依 §6 計算正確；全部完成後面板歸檔。
8. 所有設定與進度經 localStorage 持久化，重整後保留。
9. 純 vanilla JS，無新增外部相依，單檔可部署，未改動既有報價流程。

---

## 10. 邊界情況（Edge Cases）

- 某標的報價缺失／過期：該塊佔比以可得資料計算，並在 UI 標示「報價缺失」而非崩潰。
- 尚無任何持倉／core 為空：儀表板顯示引導文案（請先於設定頁完成分層對應），不得除以零。
- 使用者未分類的持倉：預設排除並提示。
- `startDate` 在未來：本月待投為 0，進度為 0。
- `completedBatches` 不得超過 `months`；達上限後「完成本月」停用。
- 帶寬設為 0：任何偏離都視為穿帶（允許，但提示可能過度敏感）。
- 資料 schema 升級：以 `version` 欄位判斷並保留向後相容的遷移邏輯。

---

## 11. 未來擴充（可選、非本次範圍）

- 偏離帶「含年度再平衡 vs 不含」的切換檢視。
- 建倉完成後自動把該塊納入再平衡監控的引導。
- 匯出／備份設定與進度（JSON）。

---

*本規格描述行為與資料結構，不涉及投資建議。實作時如與既有 app 的資料存取層有衝突，以既有機制為準並在此文件回註調整。*
